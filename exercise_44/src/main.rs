enum Mouse {
    LeftClick,
    RightClick,
    MiddleClick,
}

fn main() {
    let _numbers: Vec<i32> = vec![1, 2, 3];
    let _letters: Vec<char> = vec!['a', 'b'];
    let _clicks: Vec<Mouse> = vec![
        Mouse::LeftClick,
        Mouse::RightClick,
        Mouse::MiddleClick,
    ];
}
