fn main() {
    let coord = (2, 3);
    println!("{}, {}", coord.0, coord.1);

    let (x, y) = (2, 3);
    println!("{}, {}", x, y);

    let (_name, _age) = ("Emma", 20);

    let _favorites = ("red", 14, "TX", "pizza", "TV SHOW", "home");
}
